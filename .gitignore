node_modules
dist
.env
.env.local
.env.production
# .env.development
*.log
.DS_Store
coverage
playwright-report
test-results
.tsc-audit/

# Agent worktrees are separate git repos; they must never be committed here.
.claude/worktrees/
